\
/* ===== Canal Som / Cadastro de Veículos ===== */
.vehicle-form-page {
    max-width: 1180px;
    margin: 0 auto;
}

.page-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 24px;
}

.back-link,
.secondary-link,
.primary-link {
    text-decoration: none;
}

.back-link {
    color: #178bc7;
    font-weight: 600;
}

.vehicle-form {
    padding: 28px;
}

.vehicle-form h2 {
    margin: 0 0 18px;
}

.vehicle-form hr {
    border: 0;
    border-top: 1px solid #e7ebf0;
    margin: 30px 0;
}

.form-grid,
.form-grid-inner {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 18px;
}

.field.full,
.form-grid-inner.full {
    grid-column: 1 / -1;
}

.field label {
    display: block;
    margin: 0 0 7px;
    font-weight: 600;
}

.field input,
.field select,
.field textarea {
    width: 100%;
    min-height: 44px;
    padding: 11px 13px;
    border: 1px solid #d8dee7;
    border-radius: 8px;
    background: #fff;
    color: #172033;
    font: inherit;
}

.field textarea {
    min-height: 110px;
    resize: vertical;
}

.field input:focus,
.field select:focus,
.field textarea:focus {
    outline: 2px solid rgba(23, 139, 199, .18);
    border-color: #178bc7;
}

.field small {
    display: block;
    margin-top: 6px;
    color: #7c8798;
}

.form-actions {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    gap: 18px;
    margin-top: 28px;
}

.form-actions button {
    width: auto;
    min-width: 170px;
}

.alert ul {
    margin: 8px 0 0 20px;
}

@media (max-width: 760px) {
    .form-grid,
    .form-grid-inner {
        grid-template-columns: 1fr;
    }

    .page-header {
        align-items: flex-start;
        flex-direction: column;
    }

    .vehicle-form {
        padding: 20px;
    }
}
