import{login}from"./actions";export default async function LoginPage({searchParams}:{searchParams:Promise<{erro?:string}>}){const{erro}=await searchParams;return <main className="login-page"><form action={login} className="card login-card"><div className="brand" style={{color:"var(--foreground)",padding:0,marginBottom:24}}><span className="brand-mark">IG</span><span>InfoGate Gestão</span></div><h1 style={{margin:"0 0 8px"}}>Acesse sua conta</h1><p className="muted" style={{margin:"0 0 24px"}}>Use as credenciais cadastradas no Supabase.</p>{erro&&<p className="error">{erro}</p>}<div className="field"><label htmlFor="email">E-mail</label><input className="input" id="email" name="email" type="email" autoComplete="email" required/></div><div className="field" style={{marginTop:16}}><label htmlFor="password">Senha</label><input className="input" id="password" name="password" type="password" autoComplete="current-password" required/></div><button className="btn btn-primary" style={{width:"100%",marginTop:22}} type="submit">Entrar</button></form></main>}
