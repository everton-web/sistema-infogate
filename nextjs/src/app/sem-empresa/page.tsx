import{logout}from"../login/actions";import{bootstrapCompany}from"./actions";
export default async function NoCompanyPage({searchParams}:{searchParams:Promise<{erro?:string}>}){const{erro}=await searchParams;return <main className="login-page"><section className="card login-card"><h1>Configure sua empresa</h1><p className="muted">Este usuário ainda não pertence a uma empresa. Crie a primeira organização para começar.</p>{erro&&<p className="error">{erro}</p>}<form action={bootstrapCompany}><div className="field"><label>Nome da empresa</label><input className="input" name="name" minLength={2} required/></div><div className="field" style={{marginTop:14}}><label>Identificador</label><input className="input" name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" placeholder="minha-empresa" required/></div><button className="btn btn-primary" style={{width:"100%",marginTop:18}}>Criar empresa</button></form><form action={logout} style={{marginTop:10}}><button className="btn btn-secondary" style={{width:"100%"}}>Sair</button></form></section></main>}
